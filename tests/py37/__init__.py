"""py37 -- the directory for tests that only run on py37 or later.

We configure this in `tests/conftest.py`.

The __init__ file here is necessary to avoid test name collisions with test
files in the parent dir.
"""
