version: 2
jobs:
  test:
    docker:
      - image: circleci/python:3.6.7
    steps:
      - checkout
      - run:
          name: install tox
          command: sudo pip install tox
      - run:
          name: test
          command: tox
workflows:
  version: 2
  test:
    jobs:
      - test
